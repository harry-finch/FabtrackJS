/*
  Warnings:

  - You are about to drop the column `description` on the `MachineType` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `MachineType` DROP COLUMN `description`;
