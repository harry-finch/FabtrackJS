<!doctype html>
<html lang="en" data-bs-theme="auto">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="Edit machine" />
    <title>Fabtrack - Edit <%= machine.name %></title>

    <%- include('../includes/pagehead.html') %>
  </head>

  <body class="d-flex justify-content-center bg-body-tertiary py-4">
    <%- include('../includes/light-dark.html') %>

    <main class="main-container">
      <%- include('../includes/header.html') %>

      <!-- Page Header -->
      <div class="card border-0 shadow-sm mb-4">
        <div class="card-body p-4 d-flex flex-wrap align-items-center justify-content-between gap-3">
          <div class="d-flex align-items-center gap-3">
            <div class="icon-box bg-primary-subtle text-primary rounded-circle d-flex align-items-center justify-content-center shadow-sm" style="width: 48px; height: 48px; font-size: 1.25rem;">
              <i class="fa-solid fa-pen-to-square"></i>
            </div>
            <div>
              <div class="d-flex align-items-center gap-2">
                <h4 class="fw-bold mb-0">Modifier la machine</h4>
                <span class="badge bg-primary-subtle text-primary border border-primary-subtle rounded-pill" style="font-size: 0.72rem;">
                  <%= machine.name %>
                </span>
              </div>
              <p class="text-muted small mb-0">Paramètres techniques, maintenance et documentation machine</p>
            </div>
          </div>
          <div class="d-flex align-items-center gap-2">
            <a href="/admin/machines/manage" class="btn btn-outline-secondary btn-sm rounded-pill px-3">
              <i class="fa-solid fa-arrow-left me-1"></i> Retour aux machines
            </a>
          </div>
        </div>
      </div>

      <form action="/admin/machines/update" method="post" enctype="multipart/form-data">
        <input type="hidden" name="machineid" value="<%= machine.id %>" />

        <div class="row">
          <div class="col-6">
            <div class="form-floating mb-3">
              <input type="text" class="form-control" id="name" name="name" value="<%= machine.name %>" required />
              <label for="name"><%= typeof __ !== 'undefined' ? __('machines.field_name') : 'Nom de la machine :' %></label>
            </div>
          </div>
          <div class="col-3">
            <div class="form-floating mb-3">
              <select class="form-select" id="machinetypeId" name="machinetypeId" required>
                <% machinetypes.forEach(function(type) { %>
                  <option value="<%= type.id %>" <%= machine.machinetypeId === type.id ? 'selected' : '' %>><%= type.name %></option>
                <% }); %>
              </select>
              <label for="machinetypeId"><%= typeof __ !== 'undefined' ? __('machines.field_type') : 'Type :' %></label>
            </div>
          </div>
          <div class="col-3">
            <div class="form-floating mb-3">
              <select class="form-select" id="categoryId" name="categoryId">
                <option value=""><%= typeof __ !== 'undefined' ? __('machines.no_category') : '(Sans catégorie)' %></option>
                <% categories.forEach(function(cat) { %>
                  <option value="<%= cat.id %>" <%= machine.categoryId === cat.id ? 'selected' : '' %>><%= cat.name %></option>
                <% }); %>
              </select>
              <label for="categoryId"><%= typeof __ !== 'undefined' ? __('machines.field_category') : 'Catégorie :' %></label>
            </div>
          </div>
        </div>

        <div class="row">
          <div class="col-6">
            <div class="form-floating mb-3">
              <input type="text" class="form-control" id="make" name="make" value="<%= machine.make %>" required />
              <label for="make"><%= typeof __ !== 'undefined' ? __('machines.field_make') : 'Marque / Fabricant :' %></label>
            </div>
          </div>
          <div class="col-6">
            <div class="form-floating mb-3">
              <input type="text" class="form-control" id="model" name="model" value="<%= machine.model %>" required />
              <label for="model"><%= typeof __ !== 'undefined' ? __('machines.field_model') : 'Modèle :' %></label>
            </div>
          </div>
        </div>

        <div class="row">
          <div class="col-6">
            <div class="form-floating mb-3">
              <input type="text" class="form-control" id="serialNumber" name="serialNumber" value="<%= machine.serialNumber || '' %>" />
              <label for="serialNumber"><%= typeof __ !== 'undefined' ? __('machines.field_serial') : 'Numéro de série :' %></label>
            </div>
          </div>
          <div class="col-6">
            <div class="form-floating mb-3">
              <input type="number" class="form-control" id="internalReference" name="internalReference" value="<%= machine.internalReference || '' %>" />
              <label for="internalReference"><%= typeof __ !== 'undefined' ? __('machines.field_internal_ref') : 'Référence interne (#) :' %></label>
            </div>
          </div>
        </div>

        <div class="row">
          <div class="col-6">
            <div class="form-floating mb-3">
              <select class="form-select" id="locationId" name="locationId">
                <option value=""><%= typeof __ !== 'undefined' ? __('common.none') : 'Aucun' %></option>
                <% locations.forEach(function(loc) { %>
                  <option value="<%= loc.id %>" <%= machine.locationId === loc.id ? 'selected' : '' %>><%= loc.name %></option>
                <% }); %>
              </select>
              <label for="locationId"><%= typeof __ !== 'undefined' ? __('machines.field_location') : 'Emplacement :' %></label>
            </div>
          </div>
          <div class="col-6">
            <div class="form-floating mb-3">
              <select class="form-select" id="accessId" name="accessId" required>
                <% access.forEach(function(acc) { %>
                  <option value="<%= acc.id %>" <%= machine.accessId === acc.id ? 'selected' : '' %>><%= acc.name %></option>
                <% }); %>
              </select>
              <label for="accessId"><%= typeof __ !== 'undefined' ? __('machines.field_access') : 'Niveau d\'accès :' %></label>
            </div>
          </div>
        </div>

        <div class="row">
          <div class="col-4">
            <div class="form-floating mb-3">
              <input type="date" class="form-control" id="purchaseDate" name="purchaseDate" value="<%= machine.purchaseDate ? new Date(machine.purchaseDate).toISOString().split('T')[0] : '' %>" />
              <label for="purchaseDate"><%= typeof __ !== 'undefined' ? __('machines.field_purchase_date') : 'Date d\'achat :' %></label>
            </div>
          </div>
          <div class="col-4">
            <div class="form-floating mb-3">
              <input type="date" class="form-control" id="lastMaintenance" name="lastMaintenance" value="<%= machine.lastMaintenance ? new Date(machine.lastMaintenance).toISOString().split('T')[0] : '' %>" />
              <label for="lastMaintenance"><%= typeof __ !== 'undefined' ? __('machines.field_last_maintenance') : 'Dernière maintenance :' %></label>
            </div>
          </div>
          <div class="col-4">
            <div class="form-floating mb-3">
              <input type="date" class="form-control" id="warrantyExpiry" name="warrantyExpiry" value="<%= machine.warrantyExpiry ? new Date(machine.warrantyExpiry).toISOString().split('T')[0] : '' %>" />
              <label for="warrantyExpiry"><%= typeof __ !== 'undefined' ? __('machines.field_warranty_expiry') : 'Expiration de garantie :' %></label>
            </div>
          </div>
        </div>

        <div class="row">
          <div class="col-12">
            <div class="form-floating mb-3">
              <input type="url" class="form-control" id="documentation" name="documentation" value="<%= machine.documentation || '' %>" placeholder="https://..." />
              <label for="documentation"><%= typeof __ !== 'undefined' ? __('machines.field_documentation') : 'URL de documentation :' %></label>
            </div>
          </div>
        </div>

        <div class="row mb-4">
          <div class="col-12">
            <label class="form-label text-muted small"><%= typeof __ !== 'undefined' ? __('machines.field_picture') : 'Photo de la machine (optionnel) :' %></label>
            <input type="file" class="form-control" id="machinePicture" name="machinePicture" accept="image/*" />
            <% if (machine.picture) { %>
              <div class="mt-2">
                <span class="text-muted small">Image actuelle :</span> <img src="<%= machine.picture %>" alt="preview" style="height: 40px; border-radius: 4px;" />
              </div>
            <% } %>
          </div>
        </div>

        <div class="row">
          <div class="col-12">
            <button class="btn btn-primary w-100 mb-2 py-2" type="submit"><%= typeof __ !== 'undefined' ? __('machines.btn_update') : 'Enregistrer les modifications' %></button>
            <a href="/admin/machines/manage" class="btn btn-secondary w-100 py-2"><%= typeof __ !== 'undefined' ? __('common.cancel') : 'Annuler' %></a>
          </div>
        </div>
      </form>

      <%- include('../includes/footer.html') %>
    </main>
  </body>
</html>
