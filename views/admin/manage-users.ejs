<!doctype html>
<html lang="en" data-bs-theme="auto">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="description" content="" />
        <meta name="author" content="Stéphane Muller" />
        <title>Fabtrack administration panel</title>

        <% include ../includes/pagehead.html %>
    </head>

    <body class="d-flex justify-content-center bg-body-tertiary py-4">
        <% include ../includes/light-dark.html %>

        <main class="w-50 mx-auto">
            <% include ../includes/header.html %>

            <h4 class="mb-4"><i class="fa fa-users-gear"></i>&nbsp;&nbsp;Manage user accounts</h4>

            <table class="table">
                <thead>
                    <tr>
                        <th onclick="sortTable(0)" scope="col">#</th>
                        <th onclick="sortTable(1)" scope="col">Username</th>
                        <th onclick="sortTable(2)" scope="col">Email</th>
                        <th onclick="sortTable(3)" scope="col">Type</th>
                        <th onclick="sortTable(4)" scope="col" class="text-center">Agreement</th>
                        <th scope="col" class="text-center">Edit</th>
                        <th scope="col" class="text-center">Delete</th>
                    </tr>
                </thead>
                <tbody id="table-content">
                    <% users.forEach(function(user) { %>
                    <tr>
                        <td scope="row"><%= user.id %></th>
                        <td><%= user.name %> <%= user.surname %></td>
                        <td><a href="mailto:<%= user.mail %>"><%= user.mail %></a></td>
                        <td><%= user.usertype.name %></td>
                        <td class="text-center">
                            <% if(user.termsAndConditions) {%>
                            <i class="fa-solid fa-user-check" style="color: #44aa44"></i>
                            <% } else { %>
                            <a href="#" onclick="sendMailAgain(<%= user.id %>)" style="color: #aa4444"
                                ><i class="fa-solid fa-rotate-right"></i
                            ></a>
                            <% } %>
                        </td>
                        <td class="text-center">
                            <a href="/users/edit/<%= user.id %>"><i class="fa-solid fa-user-pen"></i></a>
                        </td>
                        <td class="text-center">
                            <a href="/users/delete/<%= user.id %>"><i class="fa-solid fa-trash-can"></i></a>
                        </td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </main>

        <script src="../javascripts/admin-scripts.js"></script>

        <% include ../includes/footer.html %>

    </body>
</html>
